# コーディング原則

## 基本原則

- **既存コードの設計思想を維持する**：実装前に既存パターンを理解し、一貫性を保つ
- **フォールバックは異常系のみ**：`??` や `||` は最終手段。正常系では確実に値が入る設計にする
  - 特にあなた(AI)は、依然としてフォールバックに頼る傾向が強いです。それは最終手段と認識し、他の選択肢を極力採用すべきです
- **安易な妥協を避ける**：「とりあえず動く」ではなく「正しく動く」を目指す
